<!DOCTYPE html>

    <%- include('../partials/head'); %>

<body class="container">
    <%- include('../partials/header'); %>

    <main>
        <div class="jumbotron container-fluid py-5 px-3 main-div">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
            <p><%= subtitle %></p>
        </div>
        <ul class="mt-4">
            <% qualitys.forEach(function(qualitys){ %>
            <li>
                <strong><%= qualitys.title %></strong><%= qualitys.message %>
            </li>
            <% }) %>
        </ul>
    </main>

    <%- include('../partials/footer'); %>

    
</body>
</html>